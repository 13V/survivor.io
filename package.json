{
  "name": "outbreak-survivors",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based top-down survivor/auto-battler (survivor.io-style). Original-asset clone.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "bitecs": "0.3.40",
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}

// Leaper — a wiry corpse that coils low before launching itself at survivors with filthy, snapping jaws.
import { registerEnemy } from '../../registry';
registerEnemy({ id: 'en21_leaper', name: 'Leaper', texKind: 1, tint: 0x8a7a4a, ai: 'charger', chargeCd: 2.6, speed: 108, hp: 80, dmg: 14, radius: 15, xp: 10, spawn: { minTime: 95, weight: 0.35 } });
